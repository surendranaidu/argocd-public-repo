apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-prometheus.fullname" . }}
  labels:
    {{- include "my-prometheus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-prometheus.selectorLabels" . | nindent 4 }}
